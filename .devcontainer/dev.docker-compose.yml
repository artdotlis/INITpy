version: '3'
services:
  dev:
    build:
      context: .
      dockerfile: .devcontainer/dev.Dockerfile
    volumes:
    - .:/workspace:cached
    - ~/.gitconfig:/etc/gitconfig
    command: /bin/sh -c "sleep infinity"
